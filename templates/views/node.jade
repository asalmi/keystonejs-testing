extends ../layouts/default

block content
  .container: .row: .col-sm-10.col-sm-offset-1.col-md-8.col-md-offset-2
    article
      if !data.node
        h2 Invalid Post.
      else
        header
          h1= data.node.title
        .post
          != data.node.content.full